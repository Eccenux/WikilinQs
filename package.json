{
	"name": "wikilinqs",
	"type": "commonjs",
	"version": "0.0.1",
	"scripts": {
		"test": "mocha",
		"build": "browserify src/main.js -o WikilinQs.js",
		"deploy": "node wikiploy.mjs",
		"rollout": "npm run build && npm run deploy"
	},
	"devDependencies": {
		"browserify": "17.x",
		"chai": "4.x",
		"eslint": "8.x",
		"mocha": "10.x"
	},
	"dependencies": {
		"wikiploy": "1.x"
	}
}
